global
+ load_employees(employees.csv)
+ process_timecards(timecards.csv)
+ process_receipts(receipts.csv)
+ run_payroll()

Employee
- emp_id: string
- first_name: string
- last_name: string
- address: string
- city: string
- state: string
- zipcode: string
- classification: Hourly OR Salaried or Commissioned
+ make_hourly(rate: float)
+ make_salaried(salary: float)
+ make_commissioned(salary: float, commission_rate: float)
+ issue_payment(uses compute_pay() from self.classification)

Classification:
+ compute_pay()

Hourly:
- Classification: obj
- hourly_rate: float
- timecards: list of floats
+ add_timecard(card: float, uses timecards)
+ compute_pay(uses timecards)

Salaried:
- Classification: obj
- salary: float
+ compute_pay(uses salary)

Commissioned:
- Salaried: obj
- salary: float
- commission_rate: float
- receipts: list of floats
+ add_receipt(rec: float, uses receipts)
+ remove_receipt(choiceIndex: int, manipulates receipts)
+ compute_pay(uses salaray, receipts, and commission_rate)

